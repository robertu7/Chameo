# Current local time defines the practice day

A Chameo belongs to the calendar day produced by interpreting its Photos creation time in the viewing device's current time zone. This keeps the Chameo album as the only shared record and matches the existing calendar model, while accepting that near-midnight Chameos can move between practice days when a device changes time zone; persisting a fixed practice day would require additional synchronized metadata and migration rules.
